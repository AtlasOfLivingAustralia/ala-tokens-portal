{
  "Parameters" : {
    "pBuild"               : "{{ codebuild_build_number }}",
    "pCleanBranch"         : "{{ clean_branch }}",
    "pDestEmail"           : "{{ dest_email }}",
    "pEnvironment"         : "{{ environment }}",
    "pLambdaCaller"        : "{{ lambda_caller }}",
    "pNodeVersion"         : "{{ node_version }}",
    "pSourceEmail"         : "{{ source_email }}"
  },
  "Tags" : {
    "product"     : "{{ product_name }}",
    "component"   : "{{ product_component }}",
    "environment" : "{{ environment }}",
    "branch"      : "{{ src_branch }}",
    "version"     : "{{ commit_id }}",
    "build"       : "{{ codebuild_build_number }}"
  },
  "StackPolicy" : {
    "Statement" : [
      {
        "Effect" : "Allow",
        "Action" : "Update:*",
        "Principal": "*",
        "Resource" : "*"
      }
    ]
  }
}
